---
- name: Install ctools
  apt: pkg={{ item }}
  with_items:
    - pkg-config
    - autogen
    - autoconf
    - build-essential
    - vim
    - exuberant-ctags
  sudo: yes
  tags:
    - ctools
    - packages
